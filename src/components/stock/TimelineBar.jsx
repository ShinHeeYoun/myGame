import React from 'react';

export function TimelineBar({ dayProgressRatio }) {   const openPercent = (6 / 24) * 100;   const closePercent = (24 / 24) * 100;   const currentPercent = dayProgressRatio * 100;    return (     <div style={{ marginBottom: '24px' }}>       <div style={{ position: 'relative', width: '100%', height: '12px', background: 'var(--danger)', borderRadius: '6px', overflow: 'hidden' }}>         {/* Trading hours (Green) */}         <div style={{            position: 'absolute',            left: `${openPercent}%`,            width: `${closePercent - openPercent}%`,            height: '100%',            background: 'var(--success)'          }} />                  {/* Current Time Marker */}         <div style={{           position: 'absolute',           left: `${currentPercent}%`,           top: 0,           width: '4px',           height: '100%',           background: '#fff',           boxShadow: '0 0 8px rgba(255,255,255,0.8)',           transform: 'translateX(-50%)',           transition: 'left 1s linear'         }} />       </div>       <div style={{ position: 'relative', width: '100%', height: '20px', fontSize: '0.8rem', color: 'var(--text-muted)', marginTop: '4px' }}>         <span style={{ position: 'absolute', left: '0' }}>00:00</span>         <span style={{ position: 'absolute', left: `${openPercent}%`, transform: 'translateX(-50%)' }}>06:00 (Open)</span>         <span style={{ position: 'absolute', right: '0' }}>24:00 (Close)</span>       </div>     </div>   ); }
