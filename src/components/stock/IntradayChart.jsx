import React from 'react';

export function IntradayChart({ intradayHistory }) {   const hours = Array.from({length: 19}, (_, i) => 6 + i);   const pointsData = hours.map(h => intradayHistory[h]).filter(p => p !== undefined);   if (pointsData.length === 0) return <div style={{ width: '150px' }}></div>;      const min = Math.min(...pointsData);   const max = Math.max(...pointsData);   const range = max - min || 1;   const width = 150;   const height = 30;    const points = hours.map((h, idx) => {     const val = intradayHistory[h];     if (val === undefined) return null;     const x = (idx / 18) * width;     const y = height - ((val - min) / range) * height;     return `${x},${y}`;   }).filter(Boolean).join(' ');    const isUp = pointsData[pointsData.length - 1] >= pointsData[0];   const color = isUp ? 'var(--success)' : 'var(--danger)';    return (     <div style={{ position: 'relative', width: width + 'px' }}>       <svg width={width} height={height} style={{ overflow: 'visible' }}>         <polyline fill="none" stroke={color} strokeWidth="2" points={points} />       </svg>       <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: '0.7rem', color: 'var(--text-muted)', marginTop: '4px' }}>         <span>6</span><span>12</span><span>18</span><span>24</span>       </div>     </div>   ); }
